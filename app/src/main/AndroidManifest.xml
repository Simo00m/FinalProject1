<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finalproject1"
        tools:targetApi="31">

        <!-- Login Activity is the launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MenuActivity" android:exported="true" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".OrderUpdateActivity" />
        <activity android:name=".OrderTrackingActivity" />
        <activity android:name=".ConfirmOrderActivity" />
        <activity android:name=".MainActivity" /> <!-- Keep MainActivity if used internally, but REMOVE the launcher intent -->

    </application>

</manifest>
